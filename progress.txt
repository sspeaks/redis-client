# Ralph Progress Log
Started: Sun Feb 15 06:07:16 PM PST 2026

## Codebase Patterns
- C# benchmark is at `hask-redis-mux/comparison/benchmarks/csharp/Program.cs`, build with `dotnet build` in nix-shell
- C# REST server is at `hask-redis-mux/comparison/benchmarks/csharp/RestServer/`
- The comparison infrastructure lives on branch `ralph/comparison-fairness-and-rest-benchmarks` (not on main)
- Use `nix-shell shell.nix --run "dotnet build"` in the csharp dir for building
- RunBenchmark pattern: accepts `Func<int, Task>`, uses `Task.Run` + async lambdas + `Task.WhenAll` for concurrency
- Haskell benchmark: `Database.Redis.Command` exports typed `ping`, `set`, `get`, `del` etc. — prefer over raw `executeCommand`
- Haskell benchmark: `withClusterClient` needs explicit import from `Database.Redis.Cluster.Client`
- Pre-commit hook runs `stylish-haskell` on `.hs` files automatically

---

## 2026-02-16T02:07 - US-001
- Converted all sync StackExchange.Redis calls to async/await in Program.cs
- Main → `static async Task Main`, RunBenchmark → `async Task<Dictionary<string, object>>` with `Func<int, Task>`
- Replaced `Parallel.For` with `Task.Run` + async lambdas + `Task.WhenAll`
- Pre-population and cleanup now use `Enumerable.Range(...).Select(i => db.StringSetAsync(...))` + `Task.WhenAll`
- No .Result or .GetAwaiter().GetResult() anywhere
- Files changed: `hask-redis-mux/comparison/benchmarks/csharp/Program.cs`
- **Learnings for future iterations:**
  - StackExchange.Redis sync methods internally do async+block, causing thread pool starvation under Parallel.For — async/await avoids this
  - CreateBatch already returns Task from StringGetAsync — wrapping in async RunBenchmark is trivial
  - The branch `ralph/benchmark-fairness-v2` was created from `ralph/comparison-fairness-and-rest-benchmarks` (not main) since comparison files only exist there
---

## 2026-02-16T02:11 - US-002
- Pipeline benchmark was already converted to async as part of US-001's comprehensive async conversion
- batch.StringGetAsync() + batch.Execute() + await Task.WhenAll(tasks) already in place
- Verified build passes with `dotnet build` in nix-shell
- Files changed: `prd.json` (marked passes: true)
- **Learnings for future iterations:**
  - US-001 was thorough enough to convert ALL benchmarks including pipeline_100_gets, so US-002 was already satisfied
  - Always check if a prior story already covered the work before implementing
---

## 2026-02-16T02:12 - US-003
- Fixed Haskell PING benchmark to use cluster client (`run (ping)`) instead of direct `submitToNode` bypass
- Removed topology/masterNodes/muxPool/pingCmd special-case code (lines 127-141)
- Removed unused imports: `Control.Concurrent.STM (readTVarIO)`, `Data.Map.Strict`
- PING now goes through full ClusterCommandClient with MOVED/ASK handling, same as SET/GET/DEL
- Files changed: `hask-redis-mux/comparison/benchmarks/haskell/Main.hs`
- **Learnings for future iterations:**
  - `Database.Redis.Command` exports `ping :: (FromResp a) => m a` — no need for raw executeCommand
  - `withClusterClient` is NOT re-exported from `Database.Redis`, needs separate import from `Database.Redis.Cluster.Client`
  - The pre-commit hook runs `stylish-haskell` formatter automatically
---

## 2026-02-16T02:15 - US-004
- Renamed Haskell pipeline benchmark from `pipeline_100_gets` to `sequential_100_gets` in Main.hs
- Added `sequential_100_gets` to OPERATIONS list in 06_benchmarks.md.tmpl (kept `pipeline_100_gets` for C# only)
- Updated pipeline methodology note to honestly explain: C# uses CreateBatch (true pipelining), Haskell sends 100 sequential GETs
- Files changed: `hask-redis-mux/comparison/benchmarks/haskell/Main.hs`, `hask-redis-mux/comparison/templates/sections/06_benchmarks.md.tmpl`
- **Learnings for future iterations:**
  - The OPERATIONS list in the template controls which ops appear in comparison tables — both libraries' data is rendered for all ops, with missing data showing default placeholder
  - Haskell `mapM_` over 100 GETs is sequential (each waits for response), NOT pipelining — honest naming matters
  - Template is a Python file (.tmpl extension) — verify with `python3 -c "exec(open(...)...)"` in nix-shell
---

## 2026-02-16T02:22 - US-005
- Fixed C# REST server to use async Redis calls instead of sync
- Replaced `db.StringGet(cacheKey)` with `await db.StringGetAsync(cacheKey)`
- Replaced `db.StringSet(cacheKey, jsonData, TimeSpan)` with `await db.StringSetAsync(cacheKey, jsonData, TimeSpan)`
- Marked handler lambda as `async`
- Files changed: `hask-redis-mux/comparison/benchmarks/csharp/RestServer/Program.cs`
- **Learnings for future iterations:**
  - The REST server fix is a 3-line change (sync→async for StringGet, StringSet, and async lambda) but prevents thread pool starvation that caused 11 req/sec throughput
  - RestServer builds independently with `dotnet build` in its own directory via nix-shell
  - ASP.NET Core MapGet handlers support async lambdas natively — just add `async` keyword and use `await`
---

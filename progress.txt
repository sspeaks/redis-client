# Ralph Progress Log
Started: Wed Feb 25 12:48:19 PST 2026

## Codebase Patterns
- New Redis commands require changes in 3 places: typeclass in Command.hs, RedisCommandClient instance in Command.hs, ClusterCommandClient instance in Client.hs
- Single-node commands use `executeCommandAs [args]`, cluster keyed commands use `executeKeyedAs key [args]`
- Use `showBS` to convert Int/Double to ByteString for Redis args
- Cluster multi-key commands (lists of keys) need a `case keys of [] -> executeKeyless ...; (k:_) -> executeKeyedAs k ...` pattern
- Build with `nix-shell --run 'cabal build all'` to verify changes
- A pre-commit hook runs stylish-haskell on changed .hs files
- New typeclass methods also need implementations in StandaloneCommandClient (Standalone.hs), not just Command.hs and Cluster/Client.hs
- When adding new keyed commands, also update `requiresKeyCommands` in Cluster/Commands.hs for proper cluster routing
- The Database.Redis re-export module uses explicit import lists — new types/functions from sub-modules must be explicitly added there
- E2E tests go in test/LibraryE2E/StandaloneTests.hs; build with `nix-shell --run 'cabal build all -fe2e'`; new describe blocks at end of spec inside outermost describe

---

## 2026-02-25T20:48 - string-commands-basic
- Implemented APPEND, STRLEN, SETEX, INCRBY, DECRBY in RedisCommands typeclass, RedisCommandClient instance, and ClusterCommandClient instance
- Files changed:
  - hask-redis-mux/lib/redis-command-client/Database/Redis/Command.hs (typeclass + instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Cluster/Client.hs (cluster instance)
- **Learnings for future iterations:**
  - New typeclass methods go between existing methods in alphabetical/logical groups (string cmds near incr/decr)
  - Instance implementations follow the same order as the typeclass declaration
  - The pre-commit hook auto-formats with stylish-haskell
---

## 2026-02-25T20:51 - string-commands-advanced
- Implemented INCRBYFLOAT, GETDEL, GETEX in RedisCommands typeclass, RedisCommandClient instance, ClusterCommandClient instance, and StandaloneCommandClient instance
- Files changed:
  - hask-redis-mux/lib/redis-command-client/Database/Redis/Command.hs (typeclass + instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Cluster/Client.hs (cluster instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Standalone.hs (standalone instance)
- **Learnings for future iterations:**
  - StandaloneCommandClient in Standalone.hs also implements RedisCommands and needs new methods (uses `submitMuxAs` instead of `executeCommandAs` or `executeKeyedAs`)
  - Previous story left Standalone.hs missing methods (append, strlen, setex, incrby, decrby); added them here to clear warnings
  - The `getex` command takes optional args as `[ByteString]` so callers control options like `["EX", "60"]` or `["PERSIST"]`
---

## 2026-02-25T20:53 - hash-commands
- Implemented HGETALL, HLEN, HSETNX, HINCRBY, HINCRBYFLOAT in RedisCommands typeclass, RedisCommandClient instance, ClusterCommandClient instance, and StandaloneCommandClient instance
- Files changed:
  - hask-redis-mux/lib/redis-command-client/Database/Redis/Command.hs (typeclass + instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Cluster/Client.hs (cluster instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Standalone.hs (standalone instance)
- **Learnings for future iterations:**
  - Hash commands with numeric args (hincrby, hincrbyfloat) need showBS for the amount parameter
  - All 3 files (Command.hs, Client.hs, Standalone.hs) must be updated together — the pattern is well established now
  - The pre-commit hook runs stylish-haskell automatically on changed files
---

## 2026-02-25T20:54 - list-commands
- Implemented LINSERT, LSET, LTRIM, LREM in RedisCommands typeclass, RedisCommandClient instance, ClusterCommandClient instance, and StandaloneCommandClient instance
- Files changed:
  - hask-redis-mux/lib/redis-command-client/Database/Redis/Command.hs (typeclass + instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Cluster/Client.hs (cluster instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Standalone.hs (standalone instance)
- **Learnings for future iterations:**
  - linsert uses ByteString for position arg ("BEFORE"/"AFTER") matching the pattern of letting callers pass Redis keywords directly
  - All 4 list commands added after lindex in each file — maintains logical grouping
  - Pattern is now very well established: add to typeclass, then 3 instances (Command.hs, Client.hs, Standalone.hs)
---

## 2026-02-25T20:56 - set-commands
- Implemented SREM, SDIFF, SINTER, SUNION, SPOP, SRANDMEMBER in RedisCommands typeclass, RedisCommandClient instance, ClusterCommandClient instance, and StandaloneCommandClient instance
- Files changed:
  - hask-redis-mux/lib/redis-command-client/Database/Redis/Command.hs (typeclass + instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Cluster/Client.hs (cluster instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Standalone.hs (standalone instance)
- **Learnings for future iterations:**
  - Multi-key set commands (sdiff, sinter, sunion) need the empty-list pattern in cluster: `case keys of [] -> executeKeyless ...; (k:_) -> executeKeyedAs k ...`
  - Single-key set commands (srem, spop, srandmember) use the standard keyed pattern
  - srem takes a list of members like sadd does
  - Pattern is fully established: 4 places to update (typeclass, Command.hs instance, Client.hs cluster instance, Standalone.hs instance)
---

## 2026-02-25T20:59 - sorted-set-commands-advanced
- Implemented ZCOUNT, ZINCRBY, ZRANGESTORE in RedisCommands typeclass, RedisCommandClient instance, ClusterCommandClient instance, and StandaloneCommandClient instance
- Files changed:
  - hask-redis-mux/lib/redis-command-client/Database/Redis/Command.hs (typeclass + instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Cluster/Client.hs (cluster instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Standalone.hs (standalone instance)
- **Learnings for future iterations:**
  - zcount uses ByteString for min/max to support Redis -inf/+inf/( syntax
  - zrangestore takes optional args as [ByteString] for BYSCORE, REV, LIMIT etc — same pattern as getex
  - zincrby uses Double for increment, needs showBS conversion
  - Pattern remains consistent: 4 places (typeclass, Command.hs, Client.hs, Standalone.hs), stylish-haskell auto-formats on commit
---

## 2026-02-25T20:57 - sorted-set-commands-basic
- Implemented ZREM, ZCARD, ZSCORE, ZRANK, ZREVRANK in RedisCommands typeclass, RedisCommandClient instance, ClusterCommandClient instance, and StandaloneCommandClient instance
- Files changed:
  - hask-redis-mux/lib/redis-command-client/Database/Redis/Command.hs (typeclass + instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Cluster/Client.hs (cluster instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Standalone.hs (standalone instance)
- **Learnings for future iterations:**
  - Sorted set commands follow the same 4-place pattern (typeclass, Command.hs, Client.hs, Standalone.hs)
  - zrem takes a list of members like srem/sadd pattern
  - zscore, zrank, zrevrank are simple key+member commands
  - Build succeeded cleanly with no warnings; stylish-haskell auto-formatted 3 files on commit
---

## 2026-02-25T21:01 - key-commands
- Implemented PERSIST, TYPE (as keyType), RENAME, RENAMENX, UNLINK in RedisCommands typeclass, RedisCommandClient instance, ClusterCommandClient instance, and StandaloneCommandClient instance
- Files changed:
  - hask-redis-mux/lib/redis-command-client/Database/Redis/Command.hs (typeclass + instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Cluster/Client.hs (cluster instance)
  - hask-redis-mux/lib/cluster/Database/Redis/Standalone.hs (standalone instance)
- **Learnings for future iterations:**
  - keyType is named to avoid conflict with Haskell's `type` keyword, but sends "TYPE" to Redis
  - unlink takes a list of keys and needs the empty-list pattern in cluster (same as sdiff/sinter/sunion)
  - rename/renamenx are simple two-key commands; in cluster they're keyed on the first key
  - Pattern remains: 4 places (typeclass, Command.hs, Client.hs, Standalone.hs), stylish-haskell auto-formats on commit
---

## 2026-02-25T21:12 - update-exports
- Updated requiresKeyCommands in Database.Redis.Cluster.Commands to include all 29 new keyed commands, organized by category (string, key, hash, list, set, sorted set, HyperLogLog)
- Updated Database.Redis re-export module to include missing Geo types (GeoUnit, GeoRadiusFlag, GeoSearchFrom, GeoSearchBy, GeoSearchOption), missing Standalone exports (defaultStandaloneConfig, runRedis, withStandaloneClient), and missing Cluster.Client export (withClusterClient)
- Files changed:
  - hask-redis-mux/lib/cluster/Database/Redis/Cluster/Commands.hs (requiresKeyCommands list)
  - hask-redis-mux/lib/redis/Database/Redis.hs (re-export imports)
- **Learnings for future iterations:**
  - When adding new Redis commands, also update requiresKeyCommands in Cluster/Commands.hs for proper cluster routing
  - The Database.Redis module uses explicit import lists (not wildcard imports), so new types/functions from sub-modules must be explicitly added
  - RedisCommands (..) already covers all typeclass methods, so new command methods don't need separate import entries
  - The module's doc comments reference runRedis, defaultStandaloneConfig, withClusterClient which must be imported for the re-export to work
  - stylish-haskell auto-formats on commit via pre-commit hook
---

## 2026-02-25T21:18 - test-string-commands
- Added E2E tests for APPEND, STRLEN, SETEX, INCRBY, DECRBY, INCRBYFLOAT, GETDEL, GETEX in test/LibraryE2E/StandaloneTests.hs
- Added new "New string commands" describe block at end of spec with 7 test cases
- Files changed:
  - test/LibraryE2E/StandaloneTests.hs (added 72 lines of tests)
- **Learnings for future iterations:**
  - E2E tests follow the pattern: createTestStandaloneClient, run commands, assert with shouldBe against RespData constructors, cleanup with del, closeStandaloneClient
  - Build E2E tests with `nix-shell --run 'cabal build all -fe2e'` (the -fe2e flag enables the e2e build targets)
  - TTL assertions use `shouldSatisfy (> 0)` since exact value depends on timing
  - New describe blocks go at the end of the spec, inside the outermost "Standalone Multiplexed Client" describe
  - stylish-haskell auto-formats on commit via pre-commit hook
---

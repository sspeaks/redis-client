# Ralph Progress Log
Started: Sun Feb 15 11:49:08 AM PST 2026
---

## Codebase Patterns
- `hask-redis-mux/data` was originally a symlink to `../data`. TH `embedFile` paths resolve relative to the CWD at compile time, which cabal sets to the package directory.
- The `cabal.project` has two packages: root `redis-client.cabal` and `hask-redis-mux/` subdir.
- `nix-shell --run "cabal build all"` is the way to build when `cabal` is not on PATH directly.
- `cabal clean` may be needed when changing file layout to clear stale TH caches.
- ripgrep (`rg`) has jemalloc issues on this system; use `grep -rn` instead.
- Cabal supports `../` relative paths in `hs-source-dirs` and `c-sources` in a multi-package project; generates sdist warnings but works.
- `hask-redis-mux/lib` was a symlink (now removed); `../lib/` paths in hask-redis-mux.cabal reference root `lib/` directly.
- `hask-redis-mux/test` was a symlink to `../test` (now removed); hask-redis-mux/test/ is a real directory with unit tests only.
- All three hask-redis-mux subdirs (data, lib, test) originally used symlinks to root; all have been converted to real dirs/paths.

## 2026-02-15T19:49 - US-001
- Removed redundant root `data/` directory
- Replaced the symlink `hask-redis-mux/data -> ../data` with a real directory containing `cluster_slot_mapping.txt`
- Files changed: `data/cluster_slot_mapping.txt` (deleted), `hask-redis-mux/data` (symlink → real dir)
- **Learnings for future iterations:**
  - `hask-redis-mux/data` was a symlink to `../data`, not a real directory. Simply deleting root `data/` broke the build.
  - TH `embedFile` resolves paths relative to the CWD at compile time. Cabal sets CWD to the package directory when building.
  - After file layout changes, `cabal clean` + rebuild is needed to clear stale TH caches.
---

## 2026-02-15T19:53 - US-002
- Updated all hask-redis-mux.cabal hs-source-dirs from `lib/X` to `../lib/X` for all 6 internal libraries (resp, client, crc16, redis-command-client, cluster, redis)
- Updated c-sources for crc16 from `lib/crc16/crc16.c` to `../lib/crc16/crc16.c`
- Updated extra-source-files from `lib/crc16/crc16.c` to `../lib/crc16/crc16.c`
- Removed `hask-redis-mux/lib` symlink (was `-> ../lib`)
- Files changed: `hask-redis-mux/hask-redis-mux.cabal`, `hask-redis-mux/lib` (deleted symlink)
- **Learnings for future iterations:**
  - `hask-redis-mux/lib` was a symlink to `../lib`, not a real directory with duplicated files. The "duplication" was via symlink.
  - Cabal supports `../` relative paths in `hs-source-dirs` and `c-sources` within a cabal.project multi-package setup. It generates warnings about sdist but works correctly.
  - The root `lib/` files were previously deleted from disk but still tracked by git — needed `git checkout -- lib/` to restore them before the `../lib/` paths would resolve.
  - Always verify root `lib/` exists on disk before changing hask-redis-mux.cabal paths, as previous cleanup iterations may have removed files.
---

## 2026-02-15T20:00 - US-003
- Removed duplicate unit test files from root test/ by breaking the hask-redis-mux/test -> ../test symlink
- Created hask-redis-mux/test/ as a real directory containing unit test files (Spec.hs, ClusterSpec.hs, ClusterCommandSpec.hs, MultiplexerSpec.hs, FromRespSpec.hs, MultiplexPoolSpec.hs)
- Root test/ now only contains E2E files (E2E.hs, ClusterE2E.hs, LibraryE2E.hs, E2EHelpers.hs, ClusterE2E/, LibraryE2E/) and FillHelpersSpec.hs
- Files changed: hask-redis-mux/test (symlink deleted, real dir created), test/ (6 unit test files removed)
- **Learnings for future iterations:**
  - `hask-redis-mux/test` was a symlink to `../test`, just like `lib/` and `data/` were. All three followed the same pattern.
  - When removing files from root `test/`, they also disappear from `hask-redis-mux/test/` because of the symlink. Must break the symlink first.
  - The correct approach is: remove symlink → create real directory → copy files to new location → git rm originals.
  - redis-client.cabal only references E2E and FillHelpersSpec test files; all other unit tests belong to hask-redis-mux.
---

## 2026-02-15T20:02 - US-004
- Verified E2E files are already absent from hask-redis-mux/test/ (US-003 broke the symlink and only copied unit tests)
- Verified hask-redis-mux.cabal has no E2E references
- cabal build all succeeds, cabal test all passes
- No code changes needed; all acceptance criteria already satisfied by US-003's symlink → real directory conversion
- Files changed: prd.json (passes: true), progress.txt
- **Learnings for future iterations:**
  - When the original hask-redis-mux/test was a symlink to ../test, E2E files appeared in both locations. Breaking the symlink in US-003 and selectively copying only unit tests automatically satisfied US-004.
  - Always check if previous stories already accomplished the work before making changes.
---
